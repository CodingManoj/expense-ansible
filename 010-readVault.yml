- name: Reading secrets from vault 
  hosts: all 
  tasks:  
    - name: Reading secret 
      ansible.builtin.debug: 
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=expense-dev/data/backend token={{vaults.expense.internal}} url=http://vaults.expense.internal:8200') }}"